server:
  port: 8081
  servlet:
    context-path: /api/v1

spring:
  application:
    name: shengyouquan-community
  
  # 数据源配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shengyouquan?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
#  # Redis配置
#  redis:
#    lettuce:
#      pool:
#        max-active: 20
#        max-idle: 10
#        min-idle: 5
#        max-wait: 5000ms
  
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 1000MB
  
  # Redis配置
  redis:
    host: localhost
    port: 6379
    password:
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 5000ms
  
  # 邮件配置
  mail:
    host: smtp.qq.com
    port: 587
    username: 2857700438@qq.com
    password: vwblttlmrohmddcf
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  
  # JSON配置
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:mapper/**/*.xml

# JWT配置
jwt:
  secret: shengyouquan-secret-key-2024-12-25
  expiration: 86400000  # 24小时
  token-prefix: Bearer

# 文件存储配置
file:
  upload:
    path: D:/uploads/shengyouquan/
    max-size: 500MB
    allowed-exts: jpg,jpeg,png,gif,mp4,avi,mp3,pdf,doc,docx
  domain: http://localhost:8081

# 日志配置
logging:
  level:
    com.shengyouquan: DEBUG
    org.springframework.web: INFO
    org.mybatis: INFO

# 管理员配置
admin:
  username: admin
  password: $2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9tYjKUiFjFO2/fG  # 加密后的密码: admin123456

# 积分配置
points:
  register: 10
  post: 5
  comment: 2
  like: 1
  daily-login: 5

# 课程配置
course:
  free-trial-duration: 300  # 试看5分钟(秒)

# 商城配置
mall:
  shipping-fee: 10  # 运费
  min-order-amount: 50  # 满减门槛
